[pytest]
testpaths = tests
markers =
    boot: Boot sequence and system startup tests
    sensor: Sensor I/O and hardware interface tests
    data_integrity: Data pipeline and integrity tests
    slow: Tests with long execution time (>5s)
    hardware: Requires real hardware (skipped in mock mode)
addopts = --tb=short
junit_family = xunit2
